import 'package:supabase/supabase.dart';
import 'dart:io';

void main() async {
  final supabaseUrl = 'YOUR_SUPABASE_URL';
  final supabaseKey = 'YOUR_SUPABASE_KEY';
  // Wait, I don't have the keys. I can just write a script that I run via flutter test or flutter run on a specific dart file inside the app, but it needs env vars.
}
